# Counselor scheduling fix

Production bundle for the already-verified Counselor OS calendar/schedule editor.

Does **not** change payment, Zibal, discounts, Prisma schema, or journey steps.

## Apply on production

```bash
cd /var/www/setareganplus
bash deploy/counselor-scheduling-fix/apply-production.sh
```
